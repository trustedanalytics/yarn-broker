security:
  user.password: fake
yarn:
  provided.zip: ${hadoop.provided.zip:{}}
store:
  path: ${broker.store.znode:/broker/yarn}
cf:
  servicename: yarn
  serviceid: yarn
  baseId: ${BASE_GUID:}
metadata:
  imageUrl: ${IMAGE_URL:}